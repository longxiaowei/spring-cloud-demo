spring:
  application:
    name: consul-provider
  cloud:
    consul:
      host: 127.0.0.1    # consul 启动地址
      port: 8500         # consul 启动端口
      discovery:
        tag: tag2
        register-health-check: false
server:
  port: 8800